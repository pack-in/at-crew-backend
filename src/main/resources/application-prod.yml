spring:
  mongodb:
    uri: ${MONGODB_URI}
    database: ${MONGODB_DATABASE}
  data:
    mongodb:
      auto-index-creation: false

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}

logging:
  level:
    com.atcrew: INFO
