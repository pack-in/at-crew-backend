# 모듈 개발 로드맵

> 작성일: 2026-07-30 / 갱신: 2026-07-31 (`docs/AT-CREW_서비스기획서_전체_20260728.xlsx` 전체 대조)
> 상태: 우선순위 확정, 진행 중
> Figma UI 화면 목록(`docs/design/figma.md`)과 구현 완료 모듈(`auth`/`member`/`artwork`/`community`)을 대조해
> 미구현 영역을 정리하고 우선순위를 확정한 문서.

---

## 2026-07-31 갱신 요약

정식 서비스 기획서(요구사항 22건·정책 38건·화면 21개·사용자 플로우 17개·기능명세 147건·QA 42건 전체)를
코드/워크트리 상태와 대조해 아래를 확정한다. **기획서가 정본** — 기존 설계 문서와 상충하는 부분은
기획서 기준으로 덮어쓴다([[feedback_figma_source_of_truth]] 원칙과 동일하게 적용).

- **3번(기업 프로필), 4번(검색) 완료 처리** — 문서만 "착수 전"으로 남아있었을 뿐 실제로는 main 머지 완료.
- **5번(결제/구독) PG 확정: Polar** — 구독형(스타터 무료/프로 월·연)+단건 게시 상품(팀원모집글 업로드권한·끌어올리기) 2종. 환불·구독취소·결제수단변경은 전부 Polar 담당, 앳크루는 노출·진입·웹훅 반영만. **→ 2026-08-13 Stripe 직결로 번복(§5 참고).**
- **1번(인증) 방식 확정** — 본인 인증(성인 인증)은 **PASS 연동**, 기업 인증은 **외부 연동 없는 수동 이메일 심사**(사업자등록증을 운영 이메일로 접수→검토중/인증완료/반려 3상태 수동 처리). 그동안 "외부 서비스 연동 여부 별도 설계 필요"로 미뤄뒀던 부분이 해소됨.
- **2번(recruit) 스코프 정정** — `recruit-module-design.md` §9에서 "범위 밖(Phase 2)"으로 뺐던 **끌어올리기(48시간 상단고정, 유료 단건상품)**와 **관심 작가(기업 전용, 좋아요/최근 본)**가 기획서에는 P0 요구사항(REQ-015, REQ-017)으로 명시돼 있음 — 정본 우선 원칙에 따라 recruit 스코프로 복원.
- **신규 항목 3건 추가**(7·8·9번, 아래) — 다국어 UI, 관리자/모더레이션, 소셜 공유 메타(OG 카드). 기존 6개 항목 어디에도 속하지 않는 크로스커팅 스코프라 별도 항목으로 신설.
- **글로벌 시간대: UTC 저장으로 확정**(사용자 결정, 2026-07-31) — `global-timezone-strategy.md`가 "설계안"에서 "확정"으로 전환, §3.1~3.4(컨테이너 TZ·Jackson 직렬화·`Member.timezone`·로깅) 구현 착수. 기존 [[feedback_timezone]]의 "모든 시간은 KST" 메모는 폐기.
- **1번(인증) PASS 영구 폐기**(사용자 결정, 2026-08-12) — 법인이 미국 기준으로 재편되며 한국 전용 서비스인 PASS 연동을 하지 않기로 확정. 성인 콘텐츠 인증은 영구적으로 없고 표시 토글(OFF/ON+항상 blur) 2단계만 남는다. 남는 범위는 기업 인증(수동 이메일 심사)뿐. 상세는 §1.

---

## 확정된 착수 순서

| 순서 | 항목 | 상태 |
|---|---|---|
| 0 | MongoDB → MariaDB 마이그레이션 | **완료(P1~P6 전부)** — self-hosted EC2 배포·DNS 연결·인증/recruit/media 파이프라인 실 prod 스모크 테스트까지 완료(2026-08-10) — `docs/design/mariadb-migration-design.md` |
| 0.5 | 글로벌 시간대 UTC 전환 | **완료**(§3.1~3.4 전부 구현·병합) — `docs/design/global-timezone-strategy.md` |
| 1 | 본인/기업 인증(verification) 시스템 | **범위 축소**(2026-08-12, PASS 영구 폐기) — 기업 인증(수동 이메일 심사)만 남음, 구현 착수 전. 다른 워크트리 작업과 안 겹침 |
| 2 | recruit 모듈 | **핵심 CRUD 완료·main 병합**(PR #34) — REST Docs 테스트·검색/기업모듈 포트 연동 잔여, 아래 참고 |
| 3 | 기업 계정/프로필 모듈 | **완료**, main 머지(`ab61508`) |
| 4 | 검색 모듈 | **완료**, main 머지(Elasticsearch 기반). `RecruitSearchPort` 스텁 교체 완료(2026-08-01, PR #41) — recruit 3종 검색은 search 위임이 아닌 DB LIKE 기반으로 구현, ES 색인 이관은 별도 스코프 |
| 5 | 결제/구독(요금제) | **sandbox 구현 완료** — PG를 Stripe 직결로 변경, 통화 USD 단일. MVP 단일 요금제(PH-08) 구현 완료. 라이브 전환은 사람 작업 잔여(`plans/260813-stripe-billing/PLAN-HUMAN.md`) |
| 6 | 설정 나머지(로그아웃/비밀번호 변경 등) | **거의 완료** — 로그아웃·비밀번호 변경·비밀번호 재설정(이메일 발송 인프라 포함, 2026-08-12)·마케팅 동의 토글·성인 콘텐츠 표시 토글 구현. 본인/기업 인증은 1번, 언어 칩은 7번 스코프 |
| 7 | 다국어(i18n) UI 로컬라이제이션 | **신규**, 착수 전 |
| 8 | 관리자/모더레이션 콘솔 | **신규**, 착수 전 |
| 9 | 소셜 공유 메타(OG 카드) | **신규**, 착수 전 |

사용자 결정 이력:
1. 인증 시스템을 먼저 제대로 만들고(원래 4번 항목을 1순위로), 그다음 원래 1순위였던 recruit부터 순서대로 진행.
2. **MariaDB 마이그레이션을 그보다 먼저 끝내고 나서 착수** — 이후 신규 모듈(인증/recruit/기업 프로필/검색/결제)은 전부 MariaDB(JPA) 기준으로 설계·구현한다. MongoDB로 먼저 만들고 나중에 이관하는 경로는 채택하지 않음.

**Why:** 4개 기존 모듈(auth/member/artwork/community)에 신규 모듈까지 얹은 뒤 마이그레이션하면 이관 대상이
더 커지고, 신규 모듈을 Mongo 기준으로 설계했다가 다시 JPA로 다시 짜는 이중 작업이 발생함. 지금 마이그레이션을
끝내두면 이후 모든 신규 모듈은 처음부터 MariaDB/JPA로 한 번에 설계 가능.

---

## 0. MongoDB → MariaDB 마이그레이션

설계는 이미 완료된 상태(`docs/design/mariadb-migration-design.md`, 상태: 설계안 → 구현 착수).
ID 전략(String 유지, VARCHAR(36), 신규 레코드는 UUIDv7), 스키마 정규화, 원자 연산 재설계, Flyway,
Spring Modulith JDBC 이벤트 레지스트리 교체, ETL·컷오버 계획까지 포함. API 계약(요청/응답 DTO)은
변경하지 않는 것이 원칙.

**이 로드맵과의 관계:** 1~6번 항목(신규 모듈)은 전부 이 마이그레이션 완료 이후 착수한다. 마이그레이션
자체는 기존 4개 모듈(auth/member/artwork/community)의 영속성 계층 전환이 스코프이며 신규 기능 추가는 아님.

---

## 1. 본인/기업 인증(verification) 시스템

**Why 최우선으로 승격했는가:** artwork(성인물 게이팅) → community(성인물 게이팅) → recruit(업로드 게이팅), 기업 프로필(구인글 업로드 게이팅) 순으로 **같은 블로커에 세 번째** 부딪힘. 지금까지는 매번 "인증 필드 없음 → 클라이언트 필터로 임시 처리"로 미뤄왔는데(artwork/community 모듈 회고 참고), 이번엔 제대로 설계하기로 결정.

**PASS 본인 인증 폐기(2026-08-12, 사용자 결정)** — 법인이 미국 기준으로 재편되면서 한국 전용 서비스인 PASS 연동을 아예 하지 않기로 확정. 이건 "이번 마일스톤엔 보류"가 아니라 **영구 폐기**다. 결과적으로:
- **성인 콘텐츠 성인 인증 자체가 영구적으로 없다.** 설정-R10의 3단계(OFF/ON+미인증 blur/ON+인증 원본) 중 "인증 원본"으로 가는 경로가 없어지고, 표시 토글 2단계(OFF/ON+항상 blur)로 영구 축소된다. 만 19세 미만 별도 처리(§구 정책)도 애초에 연령을 알 방법이 없으므로 적용 불가.
- `artwork-module-design.md`/`community-module-design.md`가 "공유 블로커"로 기록해뒀던 `adultVerificationStatus` 필드+PASS/KCB 연동은 더 이상 블로커가 아니라 **아예 안 만드는 것으로 해소**됐다 — 두 문서의 해당 서술은 실제 착수 시 정정 필요.
- 이 결정은 병렬로 진행 중인 "출시 마일스톤" 워크트리(`settings-i18n-design.md` §6 R5)가 **이번 마일스톤 한정 임시 축소**로 이미 채택한 것과 결과적으로 동일한 동작이지만, 그쪽은 "PASS 나중에 붙일 수도 있음"을 열어둔 반면 여기는 **영구 결정**이다 — 그 문서가 "이번 스코프 아님" 취급하는 것도 나중에 "역시 영구 폐기였다"로 정정하면 된다.

**남는 범위는 기업 인증뿐이다:**
- **기업 인증**: 외부 API 연동 없는 **수동 심사**. 기업이 사업자등록증을 운영 이메일로 전송 → 운영자가 육안 확인 후 검토중/인증완료/반려 3상태를 수동 전환. 시스템은 상태 필드·안내 모달만 제공하면 되고 진위 확인 로직은 불필요.
- 재가입 제한(로그인-R12, 탈퇴 10일) 등 인증과 맞물리는 정책은 `docs/AT-CREW_서비스기획서_전체_20260728.xlsx` 정책 시트(로그인-R12)에 상세.
- 다른 세션과 겹치지 않음 — `company-profile-module-design.md`가 `Company.verified` 필드를 "로드맵 1번 연동 전까지 항상 false"인 미노출 스텁으로만 걸어두고 실제 심사 플로우는 명시적으로 범위 밖으로 남겨뒀다(2026-08-10, 병행 착수 당시 확인).

**필요한 것 (축소됨):**
- `Company`(또는 `Member`) 쪽에 기업 인증 상태 필드·이력 추가(검토중/완료/반려) — 이미 `Company.verified` boolean 스텁이 있으므로 상태 enum으로 대체하거나 확장.
- 운영자용 승인/반려 처리 경로 — 관리자 Role 체계가 없으므로(로드맵 8번, 아직 미착수) 정식 어드민 API가 아니라 최소한의 내부 엔드포인트나 DB 직접 처리로 시작할 가능성 높음 — 설계 시 결정 필요.

**Figma/기획서 근거:**
- `UI개편_마이페이지_기업` (node 5154:41398) — "구인글 업로드 모달_기업 인증 전", "기업 인증 후_요금제 안내 로직" 섹션
- 기획서 요구사항 REQ-018·REQ-022, 정책 설정-R08

**폐기됨(더 이상 유효하지 않은 과거 근거, 참고용으로만 남김):**
- ~~`UI개편_작품/팀원모집글/구인글/구직글 업로드` (node 4979:871) — "본인 인증 전/후" 섹션~~ — PASS 폐기로 무의미
- ~~`UI개편_커뮤니티` (node 4856:14126) — 성인 콘텐츠 접근 제한~~ — 인증 기반 접근 제한 없음, 표시 토글만
- ~~정책 설정-R07(본인 인증)~~

---

## 2. recruit 모듈

**Why 다음 순서인가:** community 모듈 구현 시점에 이미 laiteu 필드 조사가 끝나 있어(`docs/design/community-module-design.md` §9, [[project_community_module]] 메모) 설계 착수 난이도가 낮음. community의 `RecruitFeedPort`/`NoopRecruitFeedPort` 스텁을 실제 구현으로 대체하는 자연스러운 다음 단계.

**스코프:**
- 구인글(JobPosting)/팀원모집글(TeamPosting)/구직글(JobSeekingPost) CRUD — **구현 진행 중**(워크트리 `recruit-module`, JobPosting/TeamPosting 위주, JobSeekingPost·Application은 미착수)
- 지원(Application) 플로우, 지원자 관리(전체 지원자/최종 합격 이동, 이력서 다운로드)
- 마감된 공고 정책(작성일+2개월, 삭제 불가·수정만 가능)
- **끌어올리기(boost)** — 구인글·팀원모집글 각각 별도 유료 단건상품, 적용 시 48시간 상단고정, 마지막 적용 후 48시간 재적용 쿨다운. **(2026-07-31 스코프 복원, 아래 참고)**
- **관심 작가** — 기업 전용, 작가 프로필 좋아요(♡) 저장/해제 + 최근 본 작가 자동 기록, 구인구직 > 작가 관리 탭에서 검색·정렬 조회. **(2026-07-31 스코프 복원, 아래 참고)**

**2026-07-31 스코프 정정 — 끌어올리기·관심 작가 재편입:** 설계 당시(`recruit-module-design.md` §9)에는 laiteu에 끌어올리기 구현이 없고 관심 작가는 "로드맵 범위 밖"이라 판단해 Phase 2로 미뤘으나, 정식 기획서(`docs/AT-CREW_서비스기획서_전체_20260728.xlsx`) 대조 결과 **둘 다 P0 요구사항**(REQ-015 구인구직, REQ-017 구인구직 관리)으로 명시돼 있음이 확인됨. 기획서가 정본이므로 이번 스코프에 포함한다. 끌어올리기는 Polar 결제 모듈이 아직 없으므로 구매 검증 없이 API 자체는 먼저 구현하고(다른 모듈 게이팅 스텁과 동일 패턴), 실 결제 연동은 5번(결제) 모듈 완성 후 연결하는 TODO로 남긴다.

**laiteu 대비 참고사항 (이미 조사 완료, [[project_community_module]] 메모에 상세):**
- `JobPosting`: 회사정보/급여체계(연봉제·MG+RS)/이력서 필수여부/부스트(48시간)/PENDING 승인 절차
- `TeamPosting`: 승인 절차 없음(즉시 게시), 참여비용/수익배분, 부스트 없음 — **단, 앳크루 기획서는 팀원모집글에도 끌어올리기를 요구**(laiteu와 다른 지점, 위 참고)
- `JobSeekingPost`: laiteu에 대응 없는 신규 엔티티(Figma에서만 확인) — 작화스타일/선호피드백방식/작업스타일/희망단가
- 기술부채 주의: laiteu `JobApplication.artworkPublicId`가 실제로는 jobPostingPublicId를 가리키는 오명명 필드, TeamPosting 지원자 조회에 소유권 검증 누락 이력 있음 — 앳크루에서는 처음부터 바르게 설계할 것
- `WorkLocationType` enum이 JobPosting/TeamPosting에서 이름 같고 값 다르게 중복 정의돼 있었음 → 앳크루에서는 `JobWorkLocationType`/`TeamWorkLocationType`으로 이름 분리 필요
- laiteu는 커뮤니티 쿼리 성능 이슈(p95 7.3s) 이력 있음 — artwork 모듈처럼 처음부터 커서 페이지네이션+복합 인덱스로 설계

**Figma/기획서 근거:** `UI개편_구인구직(창작자/기업 둘 다)` (node 5154:41764), `UI개편_구인글세부페이지` (5154:41397), `UI개편_팀원모집글세부페이지` (5154:41765). 기획서 REQ-015~017, 정책 업로드-R10·구인구직-R02·구인글 상세 페이지-R05, 기능명세 구인구직-R04·R05·구인글 상세 페이지-R02·R06·R09.

**2026-07-31 구현 완료(main 병합, PR #34, 이슈 #33)**: JobPosting/TeamPosting/JobSeekingPost CRUD, Application 지원·지원자 관리, 끌어올리기, 관심 작가, `RecruitService` 공개 API 이관까지 전부 병합됨.
**2026-07-31 REST Docs 완료(PR #35)**. **2026-08-01 잔여 작업 전부 완료**:
- `company.CompanyRecruitPort`/`search.RecruitSearchPort` Noop 스텁 폐기, `RecruitService` 직접 주입으로 교체(PR #41, 이슈 #36). recruit↔portfolio 통합 커서 페이지네이션 구현
- 좋아요한 작가 검색어 필터는 search가 아닌 `recruit → member`(`MemberService.findIdsByKeyword`) 경로로 구현 — search에 작가 검색 인덱스가 없고 순환 의존 방지를 위한 설계 정정(PR #41, `docs/design/recruit-module-design.md` §2.7 갱신 반영)
- 최근 본 작가 자동 기록은 `ArtistProfileViewedEvent`(member 발행) → `@ApplicationModuleListener`(recruit 구독) 이벤트 연동으로 완료(PR #40, 이슈 #37)

---

## 3. 기업 계정/프로필 모듈

**Why:** recruit과 강하게 결합 — 구인글을 게시하는 주체(기업 계정)가 없으면 recruit이 반쪽짜리가 됨. recruit과 동시 진행 또는 직후 권장.

**스코프:**
- 기업 마이페이지 (Company Info Section, Career Section — 작가용과 달리 "담당 업무" 필드 없음, "삭제하기" 버튼 없음)
- 접근 권한: 비로그인/타 사용자도 열람 가능, 본인 기업 계정으로 볼 때만 수정/업로드/관리 액션 노출
- 구인글 업로드 카드 진입점

**Figma 근거:** `UI개편_마이페이지_기업` (5154:41398), `UI개편_마이페이지_기업_수정페이지` (5244:25813)

---

## 4. 검색 모듈

**Why:** 스코프가 작고 다른 모듈과 결합도가 낮아 병행 처리하기 좋음.

**스코프:** 태그 기반 검색, 검색창, 결과 없음 상태.

**Figma 근거:** `UI개편_검색` (5154:41768)

**진행 상황(2026-07-30):** 설계 완료, Elasticsearch 기반으로 구현 착수 — `docs/design/search-module-design.md` 참고.
recruit/기업 프로필과 병렬 진행 중이라 별도 워크트리(`worktree-search-module`)에서 독립 진행했다.
구인글/구직글/팀원모집글 검색은 최초 설계 시 `RecruitSearchPort` 스텁으로 남겼으나, 2026-08-01 실구현으로
교체 완료(PR #41) — DB LIKE 기반, ES 색인 이관은 별도 스코프로 보류.

---

## 5. 결제/구독(요금제) — 설정

**상태(2026-08-13):** sandbox(test mode) 범위 구현 완료. 설계는 `docs/design/billing-module-design.md`,
프론트 연동 요청서는 `docs/design/billing-frontend-integration.md`, 잔여 사람 작업은
`plans/260813-stripe-billing/PLAN-HUMAN.md`에 있다.

**PG 변경(2026-08-13): Polar → Stripe 직결.** 근거는 글로벌 확장 우선이다. Merchant of Record가 없어져
부가세·해외 VAT 신고와 환불 운영이 앳크루 사업자 몫이 됐다 — 라이브 전환 전에 세무·국내 카드 승인률을
반드시 검증해야 한다. 결제 UI는 여전히 자체 제작하지 않는다(Stripe Checkout + Customer Portal).

**통화 변경(2026-08-13): KRW → USD 단일.** 기획서 요금제 화면의 원화 표기는 무효다.

**상품 구조:**
- 구독형: 스타터(무료)/프로 월간 $5.99(정가 $11.99)/프로 연간 $59.99(정가 $119.99).
- 단건 게시 상품 **3종**: 팀원모집글 업로드권한 $39.99(끌어올리기 1회 포함)/끌어올리기 $7.99/**구인글 업로드권한 $99.99**.
  구인글은 기획서(`마이페이지_기업-R04`, `구인구직-R02`)에 유료 상품으로 명시돼 있는데 기존 로드맵에서 누락됐던 항목이며,
  가격은 기획서에 값이 없어 이번에 새로 정했다.
- 정기 결제 실패: 웹훅 기준 판별, 유예 없이 즉시 스타터 전환. `SubscriptionPaymentFailedEvent` 발행까지 구현됨 —
  **이메일 발송은 인프라 구현 완료**(2026-08-12, `com.atcrew.common.mail`, Resend 기반 — §6 참고)이므로
  이 이벤트를 구독해 `MailSender.send(to, subject, html)` 호출하는 리스너만 추가하면 된다(인프라 자체는 더 이상 블로커 아님).
- 탈퇴 시 활성 구독 즉시 취소(잔여 기간 환불 없음). Stripe Customer는 결제 이력 보존을 위해 삭제하지 않는다.

**MVP 단일 요금제 결정 (PH-08, 2026-08-18) — 완료:** MVP는 "Portfolio Pro $8" 구독 하나만 노출하기로
결정. 단건 상품 3종(팀원모집글·끌어올리기·구인글)은 **판매 중단 + 게이팅 유지**(구매 경로 없이 게시 자체가
막힘 — recruit이 이미 이 3종에 게이팅되어 있어 그대로 둔다는 의미). 프로 연간($59.99)은 카탈로그에서 숨김.
상세·구현 내역은 `plans/260813-stripe-billing/PLAN-HUMAN.md` PH-08 참고.

**구현 완료:** 카탈로그·상태 조회 API, Checkout·Customer Portal 진입 API, 웹훅(구독 생성/갱신/실패/복원/취소·단건 결제·환불),
entitlement 원장, recruit 3종 게이팅, artwork 스타터 4개 제한, 탈퇴 연동.

**미구현(후속):** 공유 포트폴리오·다국어 노출의 프로 게이팅은 해당 기능 자체가 없어 `plans/260813-pro-plan-gating/`으로 분리했다.
결제 실패 이메일 발송(인프라는 있음, 리스너만 추가하면 됨), 라이브 계정 전환, Stripe Tax 적용 여부 판단.

**Figma/기획서 근거:** `UI개편_설정` (5154:41400) 내 "요금제 및 결제 Tab bar 화면" 섹션(6230:47908). 기획서 REQ-020·021, 정책 설정-R03·R11·요금제-R01·R02·R06, 기능명세 설정-R01~R03·요금제페이지-R01·요금제-R03~R05.

---

## 6. 설정 나머지 (로그아웃/비밀번호 변경 등)

**스코프:** 로그아웃 모달, 비밀번호 변경, 인증/동의 노출 정책. member 모듈의 작은 확장 정도 — 아무 때나 끼워 넣기 좋음.

**구현 완료(2026-08-07):**
- `POST /api/auth/logout` — 요청 바디의 refresh token 폐기(멱등). access token은 상태 없는 JWT라 무효화 대상 아님
- `POST /api/auth/email/password-change` — EMAIL 계정 전용, 현재 비밀번호 확인 후 교체 + refresh token 전체 폐기
- `PATCH /api/members/me/marketing-agreement` — 마케팅 수신 동의 토글(필수 약관 동의 시각 보존)
- `PATCH /api/members/me/adult-content` — 성인 콘텐츠 표시 토글(`Member.adultContentVisible` 신규 필드, V14)

**구현 완료(2026-08-12) — 비밀번호 재설정 + 이메일 발송 인프라:** `docs/design/auth-email-custom-redesign.md` §7이 "별도 설계 필요" 초안이었던 부분을 확정·구현. 메일 프로바이더는 **Resend**로 결정(`com.atcrew.common.mail` 포트+어댑터, 다른 모듈도 재사용 가능). 토큰 링크 방식·TTL 1시간(Figma "비밀번호를 잊으셨나요?" 이메일 문구로 확정 — 초안의 30분에서 정정), SHA-256 해시 저장, 단발성, 성공 시 전 기기 로그아웃. 동일 이메일에 GOOGLE 계정만 있으면 별도 안내 메일 발송(Figma "Google 계정 가입 이메일" 화면 확인). Flyway V17.
- `POST /api/auth/email/password-reset/request` — 가입 여부 무관 항상 200 (enumeration 방지), 이메일당 5분 3회 rate limit
- `POST /api/auth/email/password-reset/confirm` — 토큰+새 비밀번호로 재설정 확정

**잔여(다른 항목 스코프):**
- 기업 인증 버튼 → 1번(수동 심사). 본인 인증(PASS)은 영구 폐기(2026-08-12, §1) — 잔여 항목 아님
- 성인 콘텐츠 접근 게이팅 — PASS 폐기로 표시 토글(OFF/ON+항상 blur) 2단계가 영구 최종 형태. "ON+인증 원본" 경로는 없음(§1)
- 보고 싶은 언어 칩(`Member.postLanguages`) → 7번(다국어)
- 카카오톡 오픈채팅 문의 → 정적 외부 링크, 백엔드 작업 없음

**Figma 근거:** `UI개편_설정` (5154:41400)

---

## 7. 다국어(i18n) UI 로컬라이제이션 — 신규(2026-07-31)

**Why 신규 항목인가:** 기존 로드맵 어디에도 명시적으로 없었으나, 기획서 REQ-019(다국어)가 P1 우선순위로 서비스 전반에 걸쳐 있고 `global-timezone-strategy.md` §5.1에서도 별도 과제로 플래그돼 있었음. 범위가 커서 단일 모듈이 아닌 크로스커팅 인프라로 별도 항목화.

**스코프 (정책 로그인-R16, 설정-R14, 데이터구조-R06 기준):**
- 서비스 UI 4개 언어(한국어/일본어/중국어/영어) 현지화 — `MessageSource`/`Accept-Language` 기반 로케일 처리 현재 전무(에러·검증 메시지 100% 하드코딩 한국어, `global-timezone-strategy.md` §5.1 참고)
- `Member.primaryLanguage`(주 사용 언어, 가입 시 필수 선택·가입 후 변경 불가) 신규 필드
- 언어 세그먼트: 주 사용 언어가 다른 사용자 간 계정·게시글 기본 비노출(홈 피드·검색·마이페이지·구인글 목록, 딥링크는 예외) — `Member.postLanguages`(게시물 언어 선택, 복수, 설정에서 확장 가능) 신규 필드
- 검색 인덱스에 언어 필터 반영 — search 모듈(4번, 완료됨) 확장 필요
- 자동 번역은 서비스 UI 현지화에만 적용, 사용자 생성 콘텐츠는 번역하지 않음

**선행 검토:** `ActiveRegion` enum이 현재 한국 행정구역 전용 — 글로벌 회원 표현을 위한 재설계 필요(`global-timezone-strategy.md` §5.2, 이번 스코프에 포함할지 별도 결정 필요).

**필수 선행조건(2026-08-13):** 다국어 **노출**은 프로 플랜 전용 혜택이다(REQ-020, 요금제-R04). 구현 시
`BillingService.hasProPlan(memberId)` 게이팅을 반드시 함께 건다 — 스타터는 활동 언어 1개로 제한하고,
다운그레이드 계정은 기존 설정을 유지하되 신규 추가만 막는다. 상세 task는 `plans/260813-pro-plan-gating/`.

---

## 8. 관리자/모더레이션 콘솔 — 신규(2026-07-31)

**Why 신규 항목인가:** recruit(`구인글 PENDING 승인/반려`), 홈 피드(`신고 누적 콘텐츠 검토`) 등 여러 모듈에서 "관리자 권한 체계 없음"을 공통으로 스텁 처리해왔는데, 기획서에서 정식 요구사항으로 두 번 이상 반복 등장해 더 이상 개별 모듈 TODO로 미루기 어려움.

**스코프 (정책 홈-R15, 기능명세 설정 인접 항목 기준):**
- 관리자 역할(Role) 체계 — member 모듈에 아직 없음
- 콘텐츠 노출 상태 5종 관리: 정상/작성자 비공개/작성자 삭제/신고 누적 검토중/운영자 숨김. 신고 3회 누적 시 자동으로 "신고 누적 검토중" 전환(임시 숨김), 운영자가 정상 복원 또는 운영자 숨김으로 확정
- 구인글 PENDING 승인/반려 처리(recruit 모듈, 현재 인증 없이 접근 가능한 스텁 상태)
- 기업 인증(1번) 검토중/완료/반려 수동 처리 화면
- **초기 버전에는 사용자 신고 접수 UI(버튼·사유 선택) 자체는 포함하지 않음** — 상태 관리·자동 전환·운영자 처리만 우선

**Figma/기획서 근거:** 기획서 정책 홈-R15(신고 3회 누적 처리), 정책 구인구직-R02·설정-R08(기업 인증 3상태).

---

## 9. 소셜 공유 메타(OG 카드) — 신규(2026-07-31)

**Why 신규 항목인가:** 작품 상세·구인글 상세·팀원모집글 상세·작가 프로필·기업 프로필·포트폴리오 등 공유 가능한 화면 전부에서 OG 카드(썸네일·제목·설명) 요구사항이 반복 등장하는데 지금까지 어떤 설계 문서에도 반영돼 있지 않았음.

**스코프:** 각 상세 페이지에 OG 메타 태그(`og:image`/`og:title`/`og:description`) 서버 렌더링 또는 메타 태그 엔드포인트 제공. 성인물(R18/G18) 콘텐츠는 썸네일을 서비스 기본 이미지로 대체. 삭제·비공개·정지 케이스는 각 모듈의 접근 제한 처리와 동일 규칙 적용.

**Figma/기획서 근거:** 기능명세 작품 상세 페이지-R08, 마이페이지_작가-R45, 마이페이지_작가-R44, 마이페이지_기업-R10, 구인글 상세 페이지-R07, 팀원 모집글 상세 페이지-R01.

---

## 이미 구현 완료 (참고)

| 모듈 | 상태 | 설계 문서 |
|---|---|---|
| auth/member | 완료 (이메일 자체인증 + Google 로그인) | `docs/design/auth-email-custom-redesign.md` |
| artwork | 완료 (작품 CRUD, 북마크, 휴지통, 이미지 업로드) — MariaDB 전환만 잔여 | `docs/design/artwork-module-design.md`, `-summary.md` |
| billing | 완료 (Stripe 구독·단건 상품·웹훅·게이팅, sandbox 범위) | `docs/design/billing-module-design.md` |
| community | 완료 (배너, 포트폴리오/작가 찾아보기 피드) | `docs/design/community-module-design.md` |
| company(기업 프로필) | 완료 (기업 정보/경력, 구인글 카드 그리드) | (설계 문서 미등록 — CLAUDE.md 문서 목록 갱신 필요) |
| search | 완료 (Elasticsearch 기반 포트폴리오 검색) | `docs/design/search-module-design.md` |

---

## 참고: 정본 문서

`docs/AT-CREW_서비스기획서_전체_20260728.xlsx` — 요구사항(REQ)·정책·화면 목록·사용자 플로우·기능/화면 명세·QA 6개 시트로 구성된 정식 서비스 기획서. 개별 모듈 설계 문서와 상충 시 이 문서가 우선한다([[feedback_figma_source_of_truth]]).
